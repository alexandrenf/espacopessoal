# Server Configuration
PORT=6001
HOST=0.0.0.0
NODE_ENV=production
SERVER_NAME=EspacoPessoal Docs Server
MAX_CONNECTIONS=100
TIMEOUT=30000

# Server User ID for Convex API calls (used when saving documents)
SERVER_USER_ID=hocus-pocus-server

# CORS Configuration
CORS_ORIGIN=https://docs.espacopessoal.com

# Convex Configuration
# Replace with your actual Convex URLs
CONVEX_URL=
CONVEX_SITE_URL=

# Development settings (for local development)
# NODE_ENV=development
# CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001
# CONVEX_URL=https://your-dev-convex-url.convex.cloud
# CONVEX_SITE_URL=https://your-dev-convex-url.convex.site

# Logging
LOG_LEVEL=info 